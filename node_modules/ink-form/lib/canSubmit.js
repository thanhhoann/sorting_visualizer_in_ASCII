"use strict";
var __spreadArray = (this && this.__spreadArray) || function (to, from) {
    for (var i = 0, il = from.length, j = to.length; i < il; i++, j++)
        to[j] = from[i];
    return to;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.canSubmit = void 0;
var canSubmit = function (form, value) {
    return form.sections
        .map(function (section) { return section.fields; })
        .reduce(function (fields1, fields2) { return __spreadArray(__spreadArray([], fields1), fields2); }, [])
        .map(function (field) { return !field.required || (value[field.name] !== undefined && value[field.name] !== ''); })
        .reduce(function (field1, field2) { return field1 && field2; }, true);
};
exports.canSubmit = canSubmit;
